ORDER CONFIRMED
===============
Thanks for your purchase. Your order #<%= @order.public_id %> has been received and payment confirmed.

ORDER SUMMARY
-------------
<% @order.order_items.each do |item| %>
  <%= item.product.title %> · <%= item.variant.title %> ×<%= item.quantity %> — €<%= format("%.2f", item.price / 100.0) %>
<% end %>

  Total: €<%= format("%.2f", @order.order_items.sum(:price) / 100.0) %>


ACCOUNT DELIVERY INSTRUCTIONS
=============================
Please read carefully before contacting me.

STEP 1 — CONTACT ME
--------------------
Discord: @dokkanarnis
Email:   dokkanarnisx@outlook.com
(Can't DM on Discord? Use email instead.)

STEP 2 — INCLUDE IN YOUR MESSAGE
---------------------------------
1. Order ID: #<%= @order.public_id %>
2. Operating system — iOS or Android
3. A Google account email and password for linking
   (Create a fresh one — don't use your personal account)

RESPONSE TIME
-------------
I try to link all accounts immediately after purchase. Google
usually triggers a security check, so you'll need to be online
to confirm it.
Allow up to 10 hours for a response — I may be offline.

IMPORTANT: I will never ask for your primary personal account
password. If anyone does, don't share it.
